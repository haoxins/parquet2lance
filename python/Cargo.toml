[package]
name = "p2l"
version = "0.0.5"
edition = "2021"

[lib]
crate-type = ["cdylib"]
name = "parquet2lance"

[dependencies]
parquet2lance = { path = "../rust" }
pyo3 = { version = "0.18", features = ["extension-module"] }
tokio = { version = "1", features = ["full"] }
